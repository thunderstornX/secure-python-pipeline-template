# Test fixtures: synthetic secret-shaped strings used by tests to verify
# that Trufflehog's path exclusion ignores this directory. These are
# deliberately INVALID -- not real, never were real, will not validate
# against any live API. Trufflehog's --only-verified will reject them.
#
# Format follows real secret patterns so detectors recognise the shape
# without producing a verified hit.

# Fake AWS access key (invalid checksum)
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY

# Fake Slack-shaped token (invalid signature)
SLACK_TOKEN=xoxb-0000000000-0000000000000-XXXXXXXXXXXXXXXXXXXXXXXX

# Fake GitHub PAT (PAT format, never issued)
GITHUB_TOKEN=ghp_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
