# Top-level tooling for the security pipeline template.
#
# These are installed by the GitHub Actions workflows and the local_scan.sh
# script. The example-project has its own pinned dependencies at
# example-project/requirements.txt -- audited separately.
#
# Pinned for reproducible CI runs; bump deliberately, not opportunistically.
#
# Note: Semgrep is intentionally NOT pinned here.  In CI it runs inside the
# returntocorp/semgrep:1.96.0 container; locally, install with
# `pip install semgrep` (it has heavyweight transitive deps that conflict
# with pip-audit if installed in the same environment).

# -- Security scanners --------------------------------------------------------
bandit==1.8.6
pip-audit==2.10.0
