# Top-level dependencies for the template's tooling and example-project
# Pinned for reproducible CI runs; bump deliberately, not opportunistically.

# Security scanners (also installed by CI workflows; pinned here for parity in local runs)
bandit==1.8.6
pip-audit==2.7.3

# Example project + evaluation script dependencies
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
bcrypt==4.2.1
python-jose[cryptography]==3.3.0
sqlalchemy==2.0.36

# Evaluation harness
gitpython==3.1.44
tabulate==0.9.0

# Test runner (used by example-project tests)
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
